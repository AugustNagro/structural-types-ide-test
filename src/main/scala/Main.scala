case class User(firstName: String, age: Int)

@main def hello: Unit =
  val userProps = props[User]
  // complete below statement as println(userProps.firstName)
  // to check auto-complete
  println(userProps.)
